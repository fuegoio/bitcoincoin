version: '2'

services:
  database:
    image: postgres:10
    environment:
      - POSTGRES_USER=bitcoin
      - POSTGRES_PASSWORD=bitcoin
    volumes:
      - ./volumes/database:/var/lib/postgresql/data
    ports:
      - 5432:5432
